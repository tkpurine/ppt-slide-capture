{
  "name": "ppt-slide-capture",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint popup.js background.js",
    "test:manifest": "node tests/manifest.test.js",
    "test:e2e": "playwright test",
    "test": "npm run lint && npm run test:manifest && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "eslint": "^8.57.0"
  }
}
